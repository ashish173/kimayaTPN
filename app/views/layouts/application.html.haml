!!!
%html
  %head
    %title Kimaya TPN
    = stylesheet_link_tag "web-app-theme/base", "web-app-theme/themes/drastic-dark/style", "web-app-theme/override", :cache => true
    = stylesheet_link_tag  "jquery.ui.accordion", "facebox", "jquery-ui-1.8.11.custom","jquery.ui.dialog" 
    = javascript_include_tag "jquery-1.5.1", "jquery-ui-min","application", "rails", "facebox", "jquery-ui-1.8.11.custom", "jquery.ui.dialog" 
    = csrf_meta_tag
  %body
    #container
      #header
        %h1
          %a{:href => "/"} Kimaya TPN
        #user-navigation
          %ul.wat-cf
            %li               
              - if user_signed_in?
                - if current_user.role?(DOCTOR)
                  = link_to raw("<span>TPN</span>"), new_tpn_path, :rel => 'facebox'
                  %a{:href => additives_path}= t("web-app-theme.logout", :default => "Administration")
                  %a{:href => display_path}= t("web-app-theme.logout", :default => "Profile")
                %a.logout{:href => destroy_user_session_path}= t("web-app-theme.logout", :default => "Logout")
              - else
                %a.logout{:href => new_user_session_path}= t("web-app-theme.logout", :default => "Login")
        #main-navigation
          %ul.wat-cf
      #wrapper.wat-cf
        .flash
          - flash.each do |type, message|
            %div{:class => "message #{type}"}
              %p= message
        = yield
        #footer
          .block
            %p
              Copyright &copy; #{Time.now.year} Kimaya TPN.
        #sidebar
          = yield :sidebar
