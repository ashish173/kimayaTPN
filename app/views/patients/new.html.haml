= content_for :submenu do
  %ul
    %li 
      = link_to "Basic Information", "#"
    %li 
      = link_to "Patient history", "#", :id => 'ph'
    %li 
      = link_to "Daily Investigation", "#", :id => 'di'
#frmPop
  .grid_12
    .module
      %h2
        %span New Patient
      .module-body
        = render :partial => 'form'
      %div{:style => "clear:both;"}
        
:javascript
  
  function register(){
      alert("Please first register the patient");
      return false;
  }

  $("#ph").click( function () {
    register();
    });
  $("#di").click( function () {
    register();
    });
